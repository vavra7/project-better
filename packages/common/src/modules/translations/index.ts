import errorsCs from './cs/common/errors.json';
import errorsEn from './en/common/errors.json';

const cs = {
  common: {
    errors: errorsCs
  }
};

const en = {
  common: {
    errors: errorsEn
  }
};

export default {
  cs,
  en
};
